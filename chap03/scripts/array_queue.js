var data = [];


data.push(1);       // 배열의 제일 뒤에 추가
console.log(data);


data.push(2);       // 배열의 제일 뒤에 추가
console.log(data);


data.push(3);       // 배열의 제일 뒤에 추가
console.log(data);

console.log(data.shift());  // 배열의 제일 첫번째 요소 추출
console.log(data);

console.log(data.shift());  // 배열의 제일 첫번째 요소 추출
console.log(data);

console.log(data.shift());  // 배열의 제일 첫번째 요소 추출
console.log(data);
